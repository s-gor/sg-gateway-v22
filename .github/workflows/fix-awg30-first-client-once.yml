name: Fix AWG3.0 first client once

on:
  push:
    branches: [fix/02206-awg30-client-selection-runtime]
    paths:
      - .github/workflows/fix-awg30-first-client-once.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/02206-awg30-client-selection-runtime
          fetch-depth: 0

      - name: Apply patch
        run: python3 scripts/apply_awg30_first_client_fix_v2.py

      - name: Remove temporary patch files and refresh integrity
        run: |
          git rm .github/workflows/fix-awg30-first-client-once.yml
          git rm scripts/apply_awg30_first_client_fix.py
          git rm scripts/apply_awg30_first_client_fix_v2.py
          python3 - <<'PY'
          import hashlib
          import subprocess
          from pathlib import Path

          paths = subprocess.check_output(
              ['git', 'ls-files'], text=True, encoding='utf-8'
          ).splitlines()
          paths = [
              path for path in paths
              if path != 'SOURCE-SHA256SUMS' and Path(path).exists()
          ]
          Path('SOURCE-SHA256SUMS').write_text(
              '\n'.join(
                  f"{hashlib.sha256(Path(path).read_bytes()).hexdigest()}  {path}"
                  for path in paths
              ) + '\n',
              encoding='utf-8',
          )
          PY

      - name: Commit fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: restore AWG3.0 first-client submission"
          git push origin HEAD:fix/02206-awg30-client-selection-runtime
