name: Run AWG31 picker patch

on:
  push:
    branches:
      - codex/awg31-client-picker

permissions:
  contents: write

jobs:
  apply:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo staged
